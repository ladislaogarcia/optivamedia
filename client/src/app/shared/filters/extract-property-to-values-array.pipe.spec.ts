import { ExtractPropertyToValuesArrayPipe } from './extract-property-to-values-array.pipe';

describe('ExtractPropertyToValuesArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractPropertyToValuesArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
